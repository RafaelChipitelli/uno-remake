{
  "name": "client",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "npm run tokens:check && tsc && vite build",
    "typecheck": "tsc --noEmit",
    "tokens:check": "node scripts/tokens-bootstrap.mjs",
    "tokens:sync": "node scripts/tokens-bootstrap.mjs --fix",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "~5.9.3",
    "vite": "^8.0.1"
  },
  "dependencies": {
    "firebase": "^12.12.1",
    "phaser": "^3.90.0",
    "socket.io-client": "^4.8.3"
  }
}
